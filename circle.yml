machine:
  node:
    version: 6.9
  environment:
    CIRCLE_ENV: true
dependencies:
  pre:
    - rm -rf node_modules
test:
  post:
    - npm run prod
    - bash <(curl -s https://codecov.io/bash)
    - npm run e2e
    - mv ./reports ${CIRCLE_ARTIFACTS}
    - mv ./reports/unit/junit ${CIRCLE_TEST_REPORTS}
